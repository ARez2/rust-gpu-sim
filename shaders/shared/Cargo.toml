[package]
name = "shared"
version = "0.0.0"
edition = "2024"
publish = false

[lints]
workspace = true

[dependencies]
spirv-std = { workspace = true }
bytemuck = { version = "1.23.2", features = ["derive"] }
